{% extends 'base.html.twig' %}

{% block title %}{{ service.title}}{% endblock %}

{% block body %}
<<<<<<< HEAD
    <article>
        <div class="contentimage card-columns">
            <img src="{{asset('uploads/'~service.images[0].name) }}" alt="{{ service.title}}" width="300x400">
            {# inserer carousel puisqu' il y ' a plusieurs images uploader#}
            <div class="card-header">{{service.title}}</div>
            <div class="card-content">
                {# <p> Activité:{{service.category }}</p>>#}
                <p>{{service.content|raw}}</p>

                {% if is_granted("ROLE_USER") %}


                        <div class="list-group">
                        <a href="{{ path("app_message") }}" class="list-group-item list-group-item active ">
                            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-envelope" viewBox="0 0 16 16">
                                <path d="M0 4a2 2 0 0 1 2-2h12a2 2 0 0 1 2 2v8a2 2 0 0 1-2 2H2a2 2 0 0 1-2-2V4zm2-1a1 1 0 0 0-1 1v.217l7 4.2 7-4.2V4a1 1 0 0 0-1-1H2zm13 2.383-4.758 2.855L15 11.114v-5.73zm-.034 6.878L9.271 8.82 8 9.583 6.728 8.82l-5.694 3.44A1 1 0 0 0 2 13h12a1 1 0 0 0 .966-.739zM1 11.114l4.758-2.876L1 5.383v5.73z"/>
                            </svg>ENVOYER UN MESSAGE
                        </a>
                        </div>

                {% else  %}

                    <div class="list-group">
                        <a href="{{ path("app_login") }}" class="list-group-item list-group-item active ">
                            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-envelope" viewBox="0 0 16 16">
                                <path d="M0 4a2 2 0 0 1 2-2h12a2 2 0 0 1 2 2v8a2 2 0 0 1-2 2H2a2 2 0 0 1-2-2V4zm2-1a1 1 0 0 0-1 1v.217l7 4.2 7-4.2V4a1 1 0 0 0-1-1H2zm13 2.383-4.758 2.855L15 11.114v-5.73zm-.034 6.878L9.271 8.82 8 9.583 6.728 8.82l-5.694 3.44A1 1 0 0 0 2 13h12a1 1 0 0 0 .966-.739zM1 11.114l4.758-2.876L1 5.383v5.73z"/>
                            </svg>ENVOYER UN MESSAGE
                        </a>
                    </div>

                {% endif %}
            </div>
=======
<div class="form_read">
<div class="container">
   
    
        <div class="grix xs2 sm3 gutter-0 card">
            {% for image in service.images %}
                <div class="primary flex vcenter fx-center">
                    <img src="{{asset('uploads/' ~ image.name) }}" alt="{{ service.title}}" width="300x400">
                </div>
            {% endfor %} 
                {# inserer carousel puisqu' il y ' a plusieurs images uploader#}
                <div class="card-header">{{service.title}}</div>
                <div class="card-content">
                    {# <p> Activité:{{service.category }}</p>>#}
                    <p>{{service.content|raw}}</p>
                </div>
        </div>  
      

    
</div>
</div><br><br>
    <div class="grix xs2 sm3 gutter-0 card">
        <div class="col-xs2 col-sm3" id="ajout-commentaire">
            <h2>Ajouter un commentaire</h2>
            {{form(commentForm)}}
        </div>
        <div class="col-xs2 col-sm3">
            <h2>Liste des commentaires</h2>
            {% for comment in service.comments %}
                {% if comment.parent == null %}
                <h3>Commentaires {{loop.index}}</h3>
                    <p><span class="text-muted"> {{comment.createdAt|ago(locale='fr')}} - {{comment.pseudo}} </span></p>
                    <div>{{comment.content}}</div><br>
                    <p><a href="#ajout-commentaire" data-reponse data-id="{{comment.id}}" class="btn btn-primary">Répondre</a></p>
                    <div class="col-md-5">
                    {# On affiche les réponses des commentaires parents juste en decalage #}
                    {% for reponse in comment.reponses %}
                        <p><span class="text-muted"> {{reponse.createdAt|ago(locale='fr')}} - {{reponse.pseudo}} </span></p>
                        <div>{{reponse.content}}</div><br>
                    {% endfor %}
                </div>
                {% endif %}
            {% endfor %}
>>>>>>> 4896607f408025eae35ccf52736a03a6f7d4e7ff
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    <script>
        window.onload = () => {
        document.querySelectorAll("[data-reponse]").forEach(element => {
            element.addEventListener("click", function(){
                document.querySelector("#comments_parentid").value = this.dataset.id;
            });
        });
    }
    </script>
{% endblock %}