{% extends 'base.html.twig' %}

{% block title %}Service index{% endblock %}

{% block body %}
  <section class="w3l-about-breadcrumb text-left">
    <div class="breadcrumb-bg breadcrumb-bg-about py-sm-5 py-4">
      <div class="container">
        <h2 class="title">Mes annonces  </h2>
        <ul class="breadcrumbs-custom-path mt-2">
          <li><a href="/">Accueil</a></li>
          <li class="active"><span class="fa fa-arrow-right mx-2" aria-hidden="true"></span> Mes annonces </li>
        </ul>
      </div>
    </div>
  </section>
  <!-- //about breadcrumb -->

            
   <!--  Work gallery section -->
 <section class="grids-1 py-5">
  <div class="grids py-lg-5 py-md-4">
      <div class="container">
          <h3 class="hny-title mb-5">Mes annonces</h3>
          <div class="row">
              {% for service in services %}
              <div class="col-lg-4 col-md-4 col-6 mt-4">
                  <div class="column">
                      <a href="blog-single.html"><img src="{{asset('assets/images/g1.jpg')}}" alt="" class="img-fluid"></a>
                      <div class="info">
                          <h3 ><a style="color : gray" href="#">{{ service.title }}</a>&nbsp;&nbsp;<a href="{{ path('service_edit', {'id': service.id}) }}"><img src="https://img.icons8.com/office/30/000000/edit.png"/></a> </h3>
                          <p>{{ service.createdAt ? service.createdAt|date('Y-m-d H:i:s') : '' }} </p>
                          <div class="dst-btm">
                            <h6 class="">{{ service.slug }} </h6>
                            <span>{{ service.content }}</span>
                          </div>
                      </div>
                  </div>
              </div>
              {% else %}
              <h1>Aucune annonce</h1>
              {% endfor %}
          </div>
      </div>
</div></section>
<!--  //Work gallery section -->
    {# <h1>Service index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>Slug</th>
                <th>Content</th>
                <th>Created_at</th>
                <th>Active</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for service in services %}
            <tr>
                <td>{{ service.id }}</td>
                <td>{{ service.title }}</td>
                <td>{{ service.slug }}</td>
                <td>{{ service.content }}</td>
                <td>{{ service.createdAt ? service.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ service.active ? 'Yes' : 'No' }}</td>
                <td>
                    <a href="{{ path('service_show', {'id': service.id}) }}">Voir</a>
                    <a href="{{ path('service_edit', {'id': service.id}) }}">Éditer</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('service_new') }}">Proposer un service</a> #}
{% endblock %}
