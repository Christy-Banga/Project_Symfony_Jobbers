{% extends 'user.html.twig' %}

    {% block title %}Modification de mot de passe  de {{ app.user.firstname }} {{ app.user.name }} {% endblock %}


{% block body %}
    <div class="grix xs4 container">

        {% for message in app.flashes("error") %}
            <div class="col-xs1">
                <div class="p-3 my-2 rounded-2 error">{{message }}</div>
            </div>
        {% endfor %}
        <div class="col-xs3">
            <h3>Modification de profil de {{ app.user.firstname }} {{ app.user.name }}</h3>
            <form method="post">
                <div class="form-field">
                    <input type="password" id="password1" class="form-control rounded-1"  />
                    <label for="password1">Mot de passe</label>
                </div>
                <div class="form-field">
                    <input type="password" id="password2" class="form-control rounded-1"  />
                    <label for="password2">Confirmé le mot de passe</label>
                </div>

                <button class="btn shadow-1 rounded-1 blue">Valider</button>
            </form>

        </div>
    </div>
{% endblock %}
