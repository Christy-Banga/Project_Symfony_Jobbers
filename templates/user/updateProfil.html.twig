{% extends 'base.html.twig' %}

    {% block title %}Modification de  profil de {{ app.user.firstname }} {{ app.user.name }} {% endblock %}


{% block body %}
    <div class="grix xs4 container">
        <div class="col-xs3">
            <h3>Modification de profil de {{ app.user.firstname }} {{ app.user.name }}</h3>
            {{ form(form) }}
        </div>
    </div>
{% endblock %}
