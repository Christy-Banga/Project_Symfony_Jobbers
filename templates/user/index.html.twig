{% extends 'base.html.twig' %}

    {% block title %} Profil de {{ app.user.firstname }} {{ app.user.name }} {% endblock %}


{% block body %}



<!-- about breadcrumb -->
  <section class="w3l-about-breadcrumb text-left">
    <div class="breadcrumb-bg breadcrumb-bg-about py-sm-5 py-4">
      <div class="container py-2">
        <h2 class="title">Mon compte</h2>
        <ul class="breadcrumbs-custom-path mt-2">
          {% block breadcrumbtitle %}
          <li><a href="/">Accueil</a></li>
          <li class="active"><span class="fa fa-arrow-right mx-2" aria-hidden="true"></span> Mon compte </li>
          {% endblock %}
        </ul>
      </div>
    </div>
  </section>
  <!-- //about breadcrumb -->
  {% block bodyuser %}
  <!-- contact-form -->
<section class="w3l-contact" id="contact">
  <div class="contact-infubd py-5">
    <div class="container py-lg-3">
      <div class="contact-grids row">

        {% for message in app.flashes('message') %}
        <div class="col-12">
            <div class="p-3 my-2 rounded-2 success">{{message }}</div>
        </div>
        {% endfor %}

        <div class="col-lg-6 contact-right">
          <div class="partners">

            <div class="cont-details">
             {#  <h6>Mon profil</h6> #}
              <p class="mt-3 mb-4"><strong>Profil de {{ app.user.firstname }} {{ app.user.name }}</strong> </p>
            </div>


            {# <div class="hours">
               <h6 class="mt-4">Email :</h6>
               <p>{{ app.user.email }}</p>
               <h6 class="mt-4">Nom :</h6>
               <p> {{ app.user.name }}</p>
               <h6 class="mt-4">Prénoms :</h6>
               <p class="margin-top">{{ app.user.firstname }}</p>
                <h6 class="mt-4">Ville :</h6>
                <p class="margin-top">{{ app.user.ville }}</p>
                <h6 class="mt-4">Commune :</h6>
                <p class="margin-top">{{ app.user.commune }}</p>
                <h6 class="mt-4">Telephone :</h6>
                <p class="margin-top">{{ app.user.Telephone }}</p>

            </div> #}

            </div>
          </div>
        </div>
        <div class="col-lg-6 mt-lg-0 mt-5 contact-left">
          <form action="https://sendmail.w3layouts.com/submitForm" method="post" class="signin-form">
            
            {#  #}<div class="text-left" style="margin-bottom : 10px">
              <a href="{{ path("user_service_modifier") }}" class="btn btn-style btn-secondary" style="width : 100%">Modifier profil</a>
            </div>

            <div class="text-left" style="margin-bottom : 10px">
              <a href="{{ path("password_modifier") }}" class="btn btn-style btn-secondary" style="width : 100%">Modifier mot de passe</a>
            </div>


             {#  <div class="text-left" style="margin-bottom : 10px">
                  <a href="/service" class="btn btn-style btn-secondary" style="width : 100%">Mes annonces</a>
              </div> #}

              <div class="text-left" style="margin-bottom : 10px">
              <a href="/message" class="btn btn-style btn-secondary" style="width : 100%">Ma messagerie</a>
            </div>



              <div class="text-left" style="margin-bottom : 10px">
                  <a href="{{ path("app_user_data") }}" class="btn btn-style btn-secondary" style="width : 100%">Voir vos informations</a>
              </div>

          </form>
        </div>

      </div>

    </div>
  </div>
</section>
<!-- /contact-form -->

  {# <div class="grix xs1 container">
    {% for message in app.flashes("message") %}
    <div class="col-xs1">
        <div class="p-3 my-2 rounded-2 success">{{message }}</div>
    </div>
    {% endfor %}

        <div class="col-xs3">
            <h5>Profil de {{ app.user.firstname }} {{ app.user.name }}</h5>
        </div>

        <div class="col-xs3">Nom : {{ app.user.name }}</div>

        <div class="col-xs3">Prénom : {{ app.user.firstname }}</div>

        <div class="col-xs3" >Email : {{ app.user.email }}</div>



    <div class="grix xs1 container">
        <div class="col-xs3">
            <a href="{{ path("user_service_modifier") }}" class="btn shadow-1 rounded-1 blue">Modifier profil</a>
            <a href="{{ path("password_modifier") }}" class="btn shadow-1 rounded-1 blue">Modifier password</a>

        </div>
    </div>



<div class="grix xs4 container ">

    <div class="col-xs3"> <h6>Annonces de:{{ app.user.firstname }} {{ app.user.name }}</h6></div>

    {% for service in app.user.services %}

    <div class="col-xs3">Nom:{{ service.title}}</div>
    {% endfor %}
</div>
</div> #}
  {% endblock %}


{% endblock %}
