{% extends 'base.html.twig' %}

    {% block title %} Voir vos informations  {{ app.user.firstname }} {{ app.user.name }} {% endblock %}


{% block body %}



    <!-- about breadcrumb -->
    <section class="w3l-about-breadcrumb text-left">
        <div class="breadcrumb-bg breadcrumb-bg-about py-sm-5 py-4">
            <div class="container py-2">
                <h2 class="title">Mon compte</h2>
                <ul class="breadcrumbs-custom-path mt-2">
                    {% block breadcrumbtitle %}
                        <li><a href="/">Accueil</a></li>
                        <li class="active"><span class="fa fa-arrow-right mx-2" aria-hidden="true"></span> Mon compte </li>
                    {% endblock %}
                </ul>
            </div>
        </div>
    </section>
    <!-- //about breadcrumb -->
    {% block bodyuser %}
        <!-- contact-form -->
        <section class="w3l-contact" id="contact">
            <div class="contact-infubd py-5">
                <div class="container py-lg-3">
                    <div class="contact-grids row">

                        {% for message in app.flashes('message') %}
                            <div class="col-12">
                                <div class="p-3 my-2 rounded-2 success">{{message }}</div>
                            </div>
                        {% endfor %}


                        <!--  Work gallery section -->
                        <section class="grids-1 py-5">
                            <div class="grids py-lg-5 py-md-4">
                                <div class="container">
                                    <p>Voir vos informations</p>
                                    <h6 class="hny-title mb-5">Annonce de {{ app.user.firstname }} {{ app.user.name }}</h6>
                                    <div class="row">
                                        {% for service in app.user.services %}
                                            <div class="col-lg-4 col-md-4 col-6 mt-4">
                                                <div class="column">
                                                    {% for image in service.images %}
                                                    <img src="{{ asset('uploads/' ~ image.name) }}" alt="{{ service.title }}" class="img-fluid">
                                                    {% endfor %}
                                                    {#<a href="blog-single.html"><img src="{{asset('assets/images/g1.jpg')}}" alt="" class="img-fluid"></a>  #}

                                                    <div class="info">
                                                        <h3 ><a style="color : gray" href="#">{{ service.title }}</a>&nbsp;&nbsp;<a href="{{ path('service_edit', {'id': service.id}) }}"><img src="https://img.icons8.com/office/30/000000/edit.png"/></a> </h3>
                                                        <p>{{ service.createdAt ? service.createdAt|date('Y-m-d H:i:s') : '' }} </p>
                                                        <div class="dst-btm">
                                                            <h6 class="">{{ service.slug }} </h6>
                                                            <span>{{ service.content }}</span>
                                                        </div>
                                                    </div>
                                                </div>
                                            </div>
                                        {% else %}
                                            <h1>Aucune annonce</h1>
                                        {% endfor %}
                                    </div>
                                </div>
                            </div>
                        </section>

                        <div class="col-lg-6 contact-right">
                            <div class="partners">

                        <div class="cont-details">
                            <h6>Vos informations</h6>
                            <p class="mt-3 mb-4">informations de {{ app.user.firstname }} {{ app.user.name }}</p>
                        </div>

                        <div class="hours">
                            <h6 class="mt-4">Email :<p>{{ app.user.email }}</p></h6>
                            {#<p>{{ app.user.email }}</p>  #}
                            <h6 class="mt-4">Nom :<p> {{ app.user.name }}</p></h6>
                            {#<p> {{ app.user.name }}</p>  #}
                            <h6 class="mt-4">Prénoms :<p class="margin-top">|{{ app.user.firstname }}</p></h6>
                             {#<p> <p class="margin-top">{{ app.user.firstname }}</p></p>  #}

                            <h6 class="mt-4">Ville :<p class="margin-top">{{ app.user.ville }}</p></h6>
                            {#<p>  <p class="margin-top">{{ app.user.ville }}</p></p>  #}
                            <h6 class="mt-4">Commune :<p class="margin-top">{{ app.user.commune }}</p></h6>
                            {# <p class="margin-top">{{ app.user.commune }}</p> #}
                            <h6 class="mt-4">Telephone :<p class="margin-top">{{ app.user.Telephone }}</p></h6>
                             {#<p class="margin-top">{{ app.user.Telephone }}</p> #}
                        </div>
                            </div>
                        </div>



















                        {#<p>{{ app.user.email }}</p>  #}




                                </div>
                            </div>
                        </div>
                        <div class="col-lg-6 mt-lg-0 mt-5 contact-left">

                        </div>

                    </div>

                </div>
            </div>
        </section>
        <!-- /contact-form -->

        {# <div class="grix xs1 container">
    {% for message in app.flashes("message") %}
    <div class="col-xs1">
        <div class="p-3 my-2 rounded-2 success">{{message }}</div>
    </div>
    {% endfor %}

        <div class="col-xs3">
            <h5>Profil de {{ app.user.firstname }} {{ app.user.name }}</h5>
        </div>

        <div class="col-xs3">Nom : {{ app.user.name }}</div>

        <div class="col-xs3">Prénom : {{ app.user.firstname }}</div>

        <div class="col-xs3" >Email : {{ app.user.email }}</div>



    <div class="grix xs1 container">
        <div class="col-xs3">
            <a href="{{ path("user_service_modifier") }}" class="btn shadow-1 rounded-1 blue">Modifier profil</a>
            <a href="{{ path("password_modifier") }}" class="btn shadow-1 rounded-1 blue">Modifier password</a>

        </div>
    </div>



<div class="grix xs4 container ">

    <div class="col-xs3"> <h6>Annonces de:{{ app.user.firstname }} {{ app.user.name }}</h6></div>

    {% for service in app.user.services %}

    <div class="col-xs3">Nom:{{ service.title}}</div>
    {% endfor %}
</div>
</div> #}
    {% endblock %}


{% endblock %}
