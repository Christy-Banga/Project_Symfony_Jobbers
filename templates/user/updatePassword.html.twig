{% extends 'user/index.html.twig' %}

{% block breadcrumbtitle %}
          <li><a href="/">Accueil</a></li>
          <li ><span class="fa fa-arrow-right mx-2" aria-hidden="true"></span><a href="/user"> Mon compte</a> </li>
           <li class="active"><span class="fa fa-arrow-right mx-2" aria-hidden="true"></span> Modifier mot de passe </li>
          {% endblock %}

{% block bodyuser %}
    <!-- contact-form -->
<section class="w3l-contact" id="contact">
  <div class="contact-infubd py-5">
    <div class="container py-lg-3">
      <div class="contact-grids row">
        <div class="col-lg-6 mt-lg-0 mt-5 contact-right">
              {% for message in app.flashes("error") %}
                <div class="col-12">
                    <div class="p-3 my-2 rounded-2 error">{{message }}</div>
                </div>
            {% endfor %}
            {{ form_start(form)}}
            {{ form_errors(form)}}
            {{ form_row(form.plainPassword)}}
              
            <button class="btn btn-style btn-secondary">Valider</button>
            {{ form_end(form)}}  
            {{ form_start(form,{'attr':{'class':'signin-form',}}) }}

            {# <div class="input-grids">
                <div class="form-group">
                    <label>Mot de passe actuel :</label>
                    {{ form_widget(form.first_options, {'attr': {'class': 'contact-input', 'placeholder' : 'Mot de passe actuel'}}) }}
                </div>
                <div class="form-group">
                    <label>Nouveau mot de passe :</label>
                    {{ form_widget(form.second_options, {'attr': {'class': 'contact-input', 'placeholder' : 'Nouveau mot de passe'}}) }}
                </div>
            
                <div class="text-right">

                    {{ form_widget(form.Valider, {'attr': {'class': 'btn btn-style btn-secondary'}}) }}
                </div>

            </div> #}


        </div>

      </div>

    </div>
  </div>
</section>
    
    {# <div class="col-xs3">
        <h3>Modification de profil de {{ app.user.firstname }} {{ app.user.name }}</h3></div>

    <div class="row">
        <div class="col">
            {{ form_start(form)}}
            {{ form_errors(form)}}
            {{ form_row(form.plainPassword)}}
             <button type="submit">valider</button> 
            <button class="btn shadow-1 rounded-1 blue">Valider</button>
            {{ form_end(form)}}
        </div>
    </div> #}
{% endblock %}
